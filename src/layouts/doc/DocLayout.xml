<templates>
  <t t-name="DocLayout">
    <div class="doc-layout flex flex-col min-h-screen">
      <!-- Header -->
      <AppHeader>
        <t t-set-slot="menu-toggle">
          <button
            t-on-click="toggleSidebar"
            class="md:hidden mr-2 p-1.5 rounded-md text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-100 dark:hover:bg-gray-700 flex items-center justify-center"
          >
            <ion-icon t-att-name="state.isSidebarOpen ? 'close-outline' : 'menu-outline'" class="text-xl"/>
          </button>
        </t>
      </AppHeader>

      <!-- Mobile overlay -->
      <t t-if="state.isSidebarOpen">
        <div class="fixed inset-0 bg-black/50 z-30 md:hidden" t-on-click="toggleSidebar"/>
      </t>

      <div class="flex flex-1">
        <!-- Sidebar -->
        <aside
          class="fixed left-0 top-14 bottom-0 w-64 bg-white dark:bg-gray-800 border-r border-gray-200 dark:border-gray-700 overflow-y-auto transition-transform duration-300 z-40"
          t-att-class="state.isSidebarOpen ? 'translate-x-0' : '-translate-x-full md:translate-x-0'"
        >
          <nav class="p-4">
            <ul class="space-y-1">
              <li t-foreach="links" t-as="link" t-key="link.href">
                <a
                  t-att-href="link.href"
                  t-att-target="link.external ? '_blank' : undefined"
                  t-on-click="closeSidebar"
                  class="block px-3 py-2 rounded-md text-sm font-medium transition-colors flex items-center justify-between"
                  t-att-class="isActive(link.href) ? 'bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-white' : 'text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 hover:text-gray-900 dark:hover:text-white'"
                >
                  <span t-esc="link.label"/>
                  <t t-if="link.external">
                    <ion-icon name="open-outline" class="text-sm"></ion-icon>
                  </t>
                </a>
              </li>
            </ul>
          </nav>
        </aside>

        <!-- Main Content -->
        <main class="flex-1 min-w-0 mt-14 min-h-screen md:ml-64">
          <div class="max-w-4xl mx-auto px-4 md:px-8 py-8">
            <t t-slot="default"/>
          </div>
        </main>
      </div>

      <!-- Footer -->
      <div class="md:ml-64">
        <AppFooter />
      </div>
    </div>
  </t>
</templates>
