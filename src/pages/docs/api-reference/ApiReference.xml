<templates>
  <t t-name="ApiReference">
    <div class="p-8">
      <article>
        <h1 class="text-4xl font-bold mb-4 text-[var(--doc-heading-color)]">API Reference</h1>
        <p class="text-lg mb-8 text-[var(--doc-text-color)]">
          Complete reference for all MetaOWL exports and functions.
        </p>

        <h2 id="boot" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">boot()</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { boot } from 'metaowl'

boot() // Auto-discovers pages from src/pages/</code></pre>

        <h2 id="routing" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Routing</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { router, navigate, push, replace, back } from 'metaowl'

router.push('/path')
router.replace('/path')
router.back()</code></pre>

        <h2 id="store" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Store</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { createStore, Store } from 'metaowl'

const store = createStore({ state, mutations, actions })
store.commit('mutationName', payload)
store.dispatch('actionName', payload)</code></pre>

        <h2 id="fetch" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Fetch</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { Fetch } from 'metaowl'

Fetch.get(url, options)
Fetch.post(url, data, options)
Fetch.put(url, data, options)
Fetch.delete(url, options)</code></pre>

        <h2 id="meta" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Meta</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { Meta } from 'metaowl'

Meta.title('Page Title')
Meta.description('Description')
Meta.ogTitle('OG Title')</code></pre>

        <h2 id="layouts" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Layouts</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { registerLayout, getLayout, setDefaultLayout, resolveLayout } from 'metaowl'

registerLayout('admin', AdminLayout)
setDefaultLayout('admin')
const Layout = getLayout('admin')

// In page component
static layout = 'admin'   // or false to disable</code></pre>

        <h2 id="cache" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Cache</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { Cache } from 'metaowl'

await Cache.set('key', { foo: 'bar' })
const value = await Cache.get('key')
await Cache.remove('key')
await Cache.clear()
const keys = await Cache.keys()</code></pre>

        <h2 id="configure-owl" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">configureOwl()</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { configureOwl } from 'metaowl'

// Call before boot()
configureOwl({
  warnIfNoStaticProps: true,
  willStartTimeout: 10000,
  translatableAttributes: ['title', 'placeholder', 'label', 'alt'],
})</code></pre>

        <h2 id="build-routes" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">buildRoutes()</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { buildRoutes } from 'metaowl'

// Converts import.meta.glob result into a route table
const routes = buildRoutes(import.meta.glob('./pages/**/*.js', { eager: true }))</code></pre>

        <h2 id="router-guards" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Router Guards</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { beforeEach, afterEach } from 'metaowl'

beforeEach((to, from, next) => {
  if (to.meta.requiresAuth &amp;&amp; !isLoggedIn) {
    next('/login')
  } else {
    next()
  }
})

afterEach((to, from) => {
  console.log('Navigated to', to.path)
})</code></pre>

        <h2 id="error-boundary" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">ErrorBoundary</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { ErrorBoundary } from 'metaowl'

const app = ErrorBoundary.wrap(AppComponent)
app.mount(document.body)

ErrorBoundary.onError((error, context) => {
  console.error(error, context)
})</code></pre>

        <h2 id="i18n" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">I18n</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { I18n } from 'metaowl'

await I18n.load({ locale: 'de', messages: { hello: 'Hallo, {name}!' } })
I18n.t('hello', { name: 'Max' })  // "Hallo, Max!"
I18n.n(1234.5, 'currency')         // "€1.234,50"
I18n.d(new Date(), 'short')        // "18.03.2026"
await I18n.setLocale('en')
console.log(I18n.locale)           // "en"</code></pre>

        <h2 id="forms" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">useForm()</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { useForm } from 'metaowl'

const form = useForm({
  schema: {
    email:    { required: true, type: 'email' },
    password: { required: true, minLength: 8 },
  },
  initialValues: { email: '', password: '' },
  onSubmit: async (values) => { /* ... */ },
})

// form.values / form.errors / form.isSubmitting / form.isValid
// form.submit() / form.reset() / form.validate()</code></pre>

        <h2 id="odoo-service" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">OdooService</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { OdooService } from 'metaowl'

OdooService.configure({ baseUrl, database, username, password })
await OdooService.authenticate()
await OdooService.searchRead('res.partner', { domain: [], fields: ['name'], limit: 10 })
await OdooService.create('res.partner', { name: 'Test' })
await OdooService.write('res.partner', [id], { name: 'Updated' })
await OdooService.unlink('res.partner', [id])
await OdooService.call('res.partner', 'action_archive', [[id]], {})</code></pre>

        <h2 id="composables" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Composables</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import {
  useAuth,
  useLocalStorage,
  useFetch,
  useDebounce,
  useThrottle,
  useWindowSize,
  useOnlineStatus,
  useAsyncState,
  useCache,
} from 'metaowl'

const { user, isLoggedIn, login, logout } = useAuth()
const theme = useLocalStorage('theme', 'light')
const { data, loading, error, refresh } = useFetch('/api/users')
const debounced = useDebounce(query, 400)
const throttledFn = useThrottle(fn, 200)
const { width, height } = useWindowSize()
const isOnline = useOnlineStatus()</code></pre>
      </article>
    </div>
  </t>
</templates>