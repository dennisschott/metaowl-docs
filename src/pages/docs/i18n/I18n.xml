<templates>
  <t t-name="I18n">
    <div class="p-8">
      <article>
        <h1 class="text-4xl font-bold mb-4 text-[var(--doc-heading-color)]">i18n / Internationalization</h1>
        <p class="text-lg mb-8 text-[var(--doc-text-color)]">
          MetaOWL includes a full-featured translation system with pluralization, interpolation, and locale switching.
        </p>

        <h2 id="loading-messages" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Loading Messages</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">Call <code>I18n.load()</code> once before <code>boot()</code>, typically in <code>src/metaowl.js</code>:</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { I18n, boot } from 'metaowl'

await I18n.load({
  locale: 'de',
  messages: {
    welcome: 'Willkommen, {name}!',
    goodbye: 'Auf Wiedersehen!',
    items: '{count, plural, one {# Artikel} other {# Artikel}}',
  },
})

boot()</code></pre>

        <h2 id="translating" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Translating in Components</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">Use <code>I18n.t()</code> in JavaScript and OWL templates:</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { I18n } from 'metaowl'

// Simple translation
I18n.t('goodbye')              // "Auf Wiedersehen!"

// With interpolation
I18n.t('welcome', { name: 'Max' })   // "Willkommen, Max!"

// With pluralization
I18n.t('items', { count: 1 })  // "1 Artikel"
I18n.t('items', { count: 5 })  // "5 Artikel"</code></pre>

        <pre class="language-xml p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-xml">&lt;!-- In OWL templates --&gt;
&lt;h1 t-esc="I18n.t('welcome', { name: state.username })"/&gt;
&lt;span t-esc="I18n.t('items', { count: state.cartItems })"/&gt;</code></pre>

        <h2 id="locale-switching" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Switching Locale</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">Switch locale at runtime — MetaOWL re-renders affected components automatically:</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { I18n } from 'metaowl'

await I18n.setLocale('en')
console.log(I18n.locale)  // "en"</code></pre>

        <h2 id="number-date-formatting" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Number &amp; Date Formatting</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">Format numbers and dates using locale-aware helpers:</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">// Number formatting
I18n.n(1234.5, 'currency')   // "€1.234,50"

// Date formatting
I18n.d(new Date(), 'short')  // "18.03.2026"</code></pre>

        <p class="mb-4 text-[var(--doc-text-color)]">Define custom number and date formats in <code>I18n.load()</code>:</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">await I18n.load({
  locale: 'de',
  messages: { ... },
  numberFormats: {
    currency: { style: 'currency', currency: 'EUR' },
  },
  dateFormats: {
    short: { day: '2-digit', month: '2-digit', year: 'numeric' },
    long:  { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' },
  },
})</code></pre>
      </article>
    </div>
  </t>
</templates>