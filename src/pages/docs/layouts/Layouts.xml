<templates>
  <t t-name="Layouts">
    <div class="p-8">
      <article>
        <h1 class="text-4xl font-bold mb-4 text-[var(--doc-heading-color)]">Layouts</h1>
        <p class="text-lg mb-8 text-[var(--doc-text-color)]">
          Layouts provide shared page structures — headers, footers, sidebars — that wrap your page components automatically.
        </p>

        <h2 id="directory-structure" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Directory Structure</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">Create a <code>layouts/</code> directory alongside your <code>pages/</code>:</p>
        <pre class="language-text p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-text">src/
  layouts/
    default/
      DefaultLayout.js
      DefaultLayout.xml
    admin/
      AdminLayout.js
      AdminLayout.xml
  pages/
    index/
      Index.js       → uses 'default' layout
    admin/
      Dashboard.js   → can use 'admin' layout</code></pre>

        <h2 id="creating-a-layout" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Creating a Layout</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">A layout component uses <code>t-slot="default"</code> to render the page content:</p>
        <pre class="language-xml p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-xml">&lt;!-- layouts/default/DefaultLayout.xml --&gt;
&lt;templates&gt;
  &lt;t t-name="DefaultLayout"&gt;
    &lt;div class="layout-default"&gt;
      &lt;header&gt;My App Header&lt;/header&gt;
      &lt;main&gt;
        &lt;t t-slot="default"/&gt;
      &lt;/main&gt;
      &lt;footer&gt;My App Footer&lt;/footer&gt;
    &lt;/div&gt;
  &lt;/t&gt;
&lt;/templates&gt;</code></pre>

        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">// layouts/default/DefaultLayout.js
import { Component } from '@odoo/owl'

export default class DefaultLayout extends Component {
  static template = 'DefaultLayout'
}</code></pre>

        <h2 id="using-a-layout" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Using a Layout</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">Set the static <code>layout</code> property on your page component. If omitted, the <code>default</code> layout is used automatically.</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { Component } from '@odoo/owl'

export default class DashboardPage extends Component {
  static template = 'DashboardPage'
  static layout = 'admin'   // uses AdminLayout
}</code></pre>

        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { Component } from '@odoo/owl'

export default class IndexPage extends Component {
  static template = 'IndexPage'
  // No layout set → 'default' layout is used automatically
}</code></pre>

        <h2 id="layouts-api" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Layouts API</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">You can programmatically manage layouts using the Layouts API:</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { registerLayout, setDefaultLayout, getLayout } from 'metaowl'

// Register a layout manually
registerLayout('admin', AdminLayout)

// Change the default layout
setDefaultLayout('admin')

// Get a layout component by name
const Layout = getLayout('admin')</code></pre>

        <h2 id="no-layout" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Disabling the Layout</h2>
        <p class="mb-4 text-[var(--doc-text-color)]">Set <code>layout</code> to <code>false</code> to render a page without any layout wrapper:</p>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">export default class LoginPage extends Component {
  static template = 'LoginPage'
  static layout = false   // full-screen, no layout
}</code></pre>
      </article>
    </div>
  </t>
</templates>