<templates>
  <t t-name="NavigationGuards">
    <div class="p-8">
      <article>
        <h1 class="text-4xl font-bold mb-4 text-[var(--doc-heading-color)]">Navigation Guards</h1>
        <p class="text-lg mb-8 text-[var(--doc-text-color)]">
          Control navigation flow with before and after guards.
        </p>

        <h2 id="before-each" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Before Each Guard</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { beforeEach, router } from 'metaowl'

// Protect routes
beforeEach(async (to, from) => {
  const isAuthenticated = await checkAuth()
  
  if (!isAuthenticated and to.path !== 'login') {
    return { name: 'login', query: { redirect: to.fullPath } }
  }
})</code></pre>

        <h2 id="after-each" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">After Each Guard</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { afterEach } from 'metaowl'

afterEach((to, from) => {
  console.log('Navigated from ' + from.path + ' to ' + to.path)
})</code></pre>

        <h2 id="cancel-navigation" class="text-xl font-semibold mt-8 mb-4 text-[var(--doc-heading-color)]">Cancel Navigation</h2>
        <pre class="language-javascript p-4 rounded-lg overflow-x-auto text-sm mb-6"><code class="language-javascript">import { cancelNavigation } from 'metaowl'

beforeEach((to, from) => {
  if (!confirm('Leave page?')) {
    cancelNavigation()
  }
})</code></pre>
      </article>
    </div>
  </t>
</templates>